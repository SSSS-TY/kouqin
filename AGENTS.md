# AGENTS.md — kouqin（《三角洲行动》口琴自动演奏宏）

> **生成说明**
>
> 本文件基于通用模板 `D:\codex\AGENTS.md` 生成，并按当前工程实际情况裁剪。
>
> 文中 `[待确认：…]` 表示该信息尚未确定，Agent **不得自行臆测填写**；需要时应先向用户确认，确认后再替换。
>
> 本文件与用户当前会话中的明确指令冲突时，以用户最新的明确指令为准；但不得违反安全红线（§8.1）。

---

## 0. 启用前检查清单

- [x] 项目类型与业务背景已填写（见 §1）
- [x] `docs/harness/PROGRESS.md`、`docs/harness/DECISIONS.md`、`docs/harness/ERRORS.md` 已创建
- [x] `docs/harness/errors/`、`docs/harness/archive/` 目录已创建
- [x] 开发流程已内联到本文件 §3（未单独创建 `docs/harness/DEVELOPMENT_FLOW.md`）
- [x] §5.2 运行时与技术栈已确认（2026-09-25）：Python 3.13.12 + PySide6 6.11.1 + AutoHotkey v2.0.23
- [x] §5.1 核心命令速查表已填入当前**确认可运行**的命令；未确认的命令一律标注 `[待确认]`
- [x] 游戏内键位与鼠标修饰键语义已实测确认（2026-09-25）：左键 −12 半音、中键 +1、右键 +12（见 §1.3）
- [ ] 曲谱主要来源格式（文本简谱 / MIDI / 图片识别）—— 待用户确认
- [x] 运行时技术路线已定：**纯 Python 单进程**（ADR-003 取代 ADR-001；P0-2 实测 Python SendInput 在游戏内生效）
- [x] git 写操作分工：Agent **不执行** `git init/add/commit/push`，由用户在本地终端执行
      （在沙盒内 `git init` 会导致命令工具全面失效，见 `docs/harness/errors/ERR-001.md`）
- [ ] 是否启用 AST / 知识图谱同步 —— 待确认（默认：未启用，本文件不含相关流程）

---

## 1. 项目概述

本工作区是一个 **Windows 平台的《三角洲行动》游戏内乐器「口琴」自动演奏宏工具源码工程**，为**单进程 Python 应用**：

1. **图形界面（PySide6）**：曲谱库管理、曲谱编辑、导入/导出、播放控制、键位与乐器行为校准。
2. **播放引擎（后台线程）**：按编译好的演奏计划执行精确时序的键盘/鼠标注入（ctypes `SendInput`）。

> 历史：最初设计为「PySide6 界面 + AHK v2 运行时」双进程（ADR-001）；2026-09-25 实测确认纯 Python 注入可用后
> 改为单进程（ADR-003）。AutoHotkey **不再是依赖**。

目标游戏：《三角洲行动》；具体客户端版本号随实验记录填写。

### 1.1 项目目标

- 让用户能**照着一份乐谱，在游戏内自动演奏**：音高正确、相对时值正确、可重复。
- 曲谱可积累、可管理：导入 / 编辑 / 导出 / 试听，逐步建成个人曲库。
- 乐器行为（键位 → 音高、鼠标修饰键语义）**可校准**：游戏口径变化时改配置即可，不改代码。
- 结果可验证：曲谱解析、演奏计划编译、音高映射有自动化测试；游戏内行为有 P0 实验与手动验收清单。
- 工程从零起步（2026-09-25 初始化），除本文件与 `docs/` 外无既有源码。

### 1.2 工作范围

- **包含**：
  - 工程骨架与模块划分
  - 曲谱数据模型、文本简谱解析、演奏计划编译（音高映射 + 时值 → 输入事件序列）
  - 播放引擎（后台线程）：计划执行、播放控制（开始/暂停/继续/停止）、进度回调
  - PySide6 图形界面：曲谱库、编辑/预览、导入/导出、播放控制、设置、校准
  - 离线验证手段：单元测试、编译计划的可读导出（dry-run），游戏内手动验收清单
- **不包含**：
  - 读写游戏进程内存、修改/替换游戏文件、hook 游戏进程
  - 绕过、干扰或探测反作弊（ACE）机制
  - 音频合成与播放（本工具只发按键，不发声）
  - 分发、自动更新、上传曲谱或遥测
  - 面向对战自动化的能力（本工具定位是乐器演奏，见 §8.1）

### 1.3 乐器行为（**2026-09-25 游戏内实测确认**）

| 输入 | 含义 | 半音 |
|------|------|------|
| `z x c v b n m` | 中音区 `1 2 3 4 5 6 7` | `0,2,4,5,7,9,11` |
| `,` | 高八度 `1̇` | `12` |
| 按住鼠标左键 | 降调（降一个八度） | `−12` |
| 按住鼠标滚轮（中键） | 半音（升半音） | `+1` |
| 按住鼠标右键 | 升调（升一个八度） | `+12` |

> 依据：游戏 UI 会标出当前按键对应的简谱（含八度点与 `#`），用户逐键读出；
> 与 `tools/p0_sendinput_demo.py` 的注入实测共同构成结论。
> **权威来源仍是 `config/instrument.json`**，代码不得硬编码上表数值。

---

## 2. 指令优先级与适用范围

Agent 执行任务时，按以下顺序解释约束：

1. 安全红线与敏感信息保护规则（§8.1）
2. 用户在当前会话中的最新明确指令
3. 本文件中的项目专属约束
4. 项目现有代码、测试、文档和代码风格
5. 通用最佳实践

出现冲突时，不得静默选择其中一种解释：应指出冲突内容、影响和建议方案；若涉及破坏性操作、公共契约或数据安全，必须暂停并请求用户确认。

---

## 3. 任务分类与流程选择

### 3.1 功能开发：执行完整四步法

满足以下任一条件时，原则上视为功能开发：

- 新增用户可感知的业务能力（新页面、新导入格式、新播放能力）
- 新增或修改运行时契约（`runtime/*.json`、`config/instrument.json` 的字段与语义）
- 引入新的外部依赖或新的输入注入方式
- 涉及多个模块且改变模块职责或交互方式
- 需求存在明显设计取舍，需要先确定方案
- 用户明确要求走完整设计、规格或 TDD 流程

功能开发严格执行：**设计 → 规格 → 测试 → 实现**

#### 快速概览

- **Step 1：设计**
  - 输出 `DESIGN_SUMMARY_FOR_OPENSPEC`（落位 `docs/design/`）
  - 门禁：用户明确回复 `APPROVED`
- **Step 2：规格**
  - 产出 `docs/design/SPEC.md`（含契约与验收标准）+ 需要时补充实验清单
  - 门禁：用户明确回复 `SPEC_APPROVED`
- **Step 3：测试**
  - 先输出 `docs/design/TEST_PLAN.md`
  - 门禁一：用户明确回复 `TEST_PLAN_APPROVED`
  - 编写测试后等待继续指令
  - 门禁二：用户明确回复 `CONTINUE`
- **Step 4：实现与归档**
  - 编写满足测试和规格的最小实现
  - 运行定向测试 + 全量回归
  - 更新 `PROGRESS.md`、`DECISIONS.md`
  - 门禁：用户明确回复 `ARCHIVE`

#### 功能开发强制纪律

- 每个门禁都必须获得用户**明确批准**，不得根据语气自行推断。
- Step 3 和 Step 4 原则上在新会话中开展，除非用户明确豁免。
- 实现必须有可验证的测试或其他明确验收方式；游戏内行为以手动验收清单为准。
- 未获得门禁批准时，只能澄清、分析或修订当前阶段产物，不得提前进入下一阶段。

### 3.2 日常维护：执行轻量级流程

- 修复不改变外部契约的局部 bug
- 修改文档、注释或示例
- 调整非核心配置，且不改变公共默认行为
- 修复拼写、格式或静态检查问题
- 在已有依赖和架构内进行局部实现调整
- 增补已有行为的测试
- 用户明确要求进行局部、外科手术式修改

即使最初判断为日常维护，若执行中发现涉及运行时契约、数据模型、输入注入方式或核心架构，必须停止轻量级流程，向用户说明并建议升级为完整四步法。

### 3.3 无法判断时

1. 用 1～3 句话说明对需求的理解。
2. 列出 2～6 条关键假设。
3. 指出建议采用「完整四步法」还是「轻量级流程」及理由。
4. 请求用户确认后再修改代码。

---

## 4. 会话标准操作流程

### 4.1 会话开始：上班打卡

1. 读取 `docs/harness/PROGRESS.md`，了解当前任务、进度与下一步。
2. 读取 `docs/harness/DECISIONS.md`，了解已接受的架构与业务决策。
3. 读取 `docs/harness/ERRORS.md`，检查是否有与当前任务相关的未解决错误。
4. 检查工作区状态：`git status --short`（若 `.git` 不可用，直接说明无法检查，不假装已检查）。
5. 确定任务，优先级从高到低：用户当前 Prompt → `PROGRESS.md` 的 `Active Task` → `Next Steps`。
6. 判断任务类型（§3），选择完整四步法或轻量级流程。
7. 需求模糊时先澄清，不直接编码。

### 4.2 开始修改前

输出简短计划，格式：

```text
1. [操作步骤] → verify: [验证方法]
2. [操作步骤] → verify: [验证方法]
```

同时明确：将修改哪些文件、哪些内容不在本次范围内、是否涉及依赖或运行时契约、成功标准、是否存在需要用户决策的分支。简单任务只列 1～3 步，不为形式制造无意义计划。

### 4.3 编码与验证循环：阶段打卡

1. 只做当前步骤所需的最小改动。
2. 运行与该改动最相关的最小测试或检查。
3. 测试失败先分析原因，不盲目扩大修改范围。
4. 通过后检查 diff：`git diff -- [相关文件]`、必要时 `git diff --check`。
5. 仅暂存本次任务相关文件（`git add <file>`），避免 `git add .`。
6. 上述 git 写操作**由用户在本地终端执行**，Agent 只输出建议命令。
   注意：**禁止在沙盒内执行 `git init`** —— 由沙盒账户创建的 `.git` 会使环境刷新失败、命令工具不可用
   （`docs/harness/errors/ERR-001.md`）。

### 4.4 任务切换

1. 将当前未完成任务写入 `PROGRESS.md` 的 `Suspended Tasks`（已完成/未完成/暂停原因/代码与测试状态/恢复下一步）。
2. 将新任务设为 `Active Task`，清空与旧任务相关的 `Blocked` 状态。
3. 向用户说明：`已暂存任务 X，现在开始处理 Y。如需恢复 X，请告知。`

### 4.5 会话意外中断后的恢复

1. 读取 `PROGRESS.md` / `DECISIONS.md` / `ERRORS.md`。
2. 执行 `git status --short`、`git log -1 --oneline`（`.git` 不可用时说明）。
3. 检查最近一次成功验证记录。
4. 不假设工作区与中断前一致。
5. 汇报：上次进行到 X；当前代码状态 Y；建议下一步 Z；是否继续？

### 4.6 会话结束：下班打卡

按 §9 统一完成检查清单执行，最低要求：运行定向测试、更新 `PROGRESS.md`、必要时更新 `DECISIONS.md`、记录未解决错误、按 §9.4 格式汇报。

---

## 5. 项目专属上下文

### 5.1 核心命令速查表

运行时：`python`（= `C:\Users\jimot\AppData\Local\Programs\Python\Python313\python.exe`，3.13.12）

- **安装依赖**：`python -m pip install -r requirements.txt`（该文件尚未创建；新增依赖需用户授权）
- **运行全部测试**：`python -m pytest -q`
- **模块测试**：`python -m pytest tests/unit -q`
- **单个测试**：`python -m pytest tests/unit/<file>.py::<test_name> -q`
- **语法检查**：`python -m compileall -q kouqin`
- **静态检查**：`[待确认：尚未选定 linter，未选定前不新增]`
- **本地启动 UI**：`python -m kouqin`（持续占用终端，**不得在主对话前台阻塞运行**）
- **本地 dry-run（不注入）**：`python -m kouqin dry-run scores\twinkle.kq`（打印事件序列，适合离线核对）
- **游戏内演奏**：`python -m kouqin play scores\twinkle.kq`（倒计时后真实注入；持续占用终端，须由用户手动执行）
- **环境自检**：`python -m kouqin check`（是否管理员、前台是否可注入）
- **注入实验工具**：`python tools\p0_sendinput_demo.py --dry-run scale`（`--help` 列出 `check`/`scale`/`hold-sweep`/`sustain`/`compare`/`mouse`）
- **P0-7 长音衰减测量**：`python tools\p0_sendinput_demo.py sustain`（依次按住 2/4/6/8/10/12 秒，报「第几段开始末尾明显变小」）
- **打包到游戏机 B**：`python tools\make_bundle.py --zip`（清单见 `docs/RUN_ON_B.md`）
- **AutoHotkey**：**不再是依赖**（ADR-003）。仅当 Python 注入路线将来失效时才作为备选，届时再补命令

#### 命令执行规则

- 执行检查命令前先确认其存在（`Get-Command <cmd>` 或 `Test-Path`）。
- 命令不存在时不得反复尝试同一无效命令，应提示用户修正，并改用不引入新依赖的保守验证（编译、已有测试、dry-run）。
- 不得擅自安装全局工具或新增项目依赖。

### 5.2 核心架构与技术栈

- **主要语言**：Python 3.13.12（界面、曲谱处理、播放引擎与输入注入，单进程）
- **界面框架**：PySide6 6.11.1
- **测试框架**：pytest 9.1.1
- **部署环境**：Windows 10/11 x64，本地单机运行，无服务端
- **外部依赖**：仅使用本机已安装组件；新增依赖（如 MIDI 解析）必须先获得用户授权并记录 ADR

### 5.3 关键入口和核心模块（Step 4 目标结构）

- `kouqin/__main__.py`：入口（有子命令时转 `kouqin.cli`，无参数时为界面）
- `kouqin/cli.py`：命令行入口（`dry-run` / `play` / `check`），图形界面完成前的实测通道
- `kouqin/core/`：纯领域逻辑（音高/音级、音高映射、曲谱模型、演奏计划编译），**不依赖 Qt、不依赖系统输入**
- `kouqin/scores/`：曲谱读写（文本简谱 DSL、JSON）、曲谱库索引
- `kouqin/ui/`：PySide6 界面（曲谱库、编辑预览、播放控制、设置、校准）
- `kouqin/input/`：SendInput 封装（扫描码键盘 + 鼠标键）、按键状态跟踪与释放
- `kouqin/player/`：播放引擎：计划执行、暂停/继续/停止、进度回调（不依赖 Qt）
- `kouqin/hotkey/`：全局热键注册与分发（ctypes `RegisterHotKey`，不依赖 Qt）
- `config/instrument.json`：乐器键位与鼠标修饰键语义（唯一权威来源）
- `config/settings.json`：界面与播放偏好
- `scores/`：曲谱库（用户数据）
- `runtime/`：运行时生成的计划/控制/状态文件（自动生成，不手工编辑、不入库）
- `tests/unit/`：领域逻辑与契约的单元测试
- `tools/`：一次性实验与排查脚本（P0 实验等），不属于产品代码
- `docs/design/`：设计、规格、测试计划、实验清单

### 5.4 项目特定约定

1. **音高与按键不得硬编码**：键位、音级、鼠标修饰键语义一律来自 `config/instrument.json`。
2. **内核纯净**：`kouqin/core/` 与 `kouqin/scores/` 是纯逻辑，禁止 import Qt、禁止直接调用系统输入。
3. **输入注入单点封装**：所有 `SendInput` 调用只允许出现在 `kouqin/input/` 内，其他模块不得直接调用系统输入 API；更改注入方式前必须先记 ADR。
4. **口径来自实测**：涉及游戏内行为的结论必须来自 P0 实验或实测记录，禁止凭推测写入文档、注释或默认配置。
5. **时间单位统一**：曲谱用「拍」、配置用「毫秒」，两者只在编译阶段换算一次，换算函数集中在一处并有单元测试。

### 5.5 禁止修改范围

除非用户明确授权，否则不得修改：

- 游戏安装目录与任何游戏文件
- `reference/`（若后续引入参考工程，只读）
- `runtime/` 下的自动生成文件（应通过重新编译/重新运行生成）
- `scores/` 中用户已有曲谱的内容（只允许通过解析器/界面新增或修改）

---

## 6. 知识库索引

- **当前进度**：`docs/harness/PROGRESS.md`
- **历史决策**：`docs/harness/DECISIONS.md`
- **错误索引**：`docs/harness/ERRORS.md`
- **错误详情目录**：`docs/harness/errors/`
- **设计与实验**：`docs/design/DESIGN_SUMMARY_FOR_OPENSPEC.md`
- **B 机（游戏机）拷贝与运行**：`docs/RUN_ON_B.md`
- **开发流程**：本文件 §3

引用文档前应确认文件存在。若必需文件缺失：不得假装已读取；应指出缺失文件及影响，并向用户索取信息。

---

## 7. 核心工作准则

### 7.1 谋定而后动

**Don't assume. Don't hide confusion. Surface tradeoffs.**

- 写代码前明确关键假设；不确定时直接提问。
- 存在多种实现路径时，说明主要选项、优缺点与推荐方案。
- 不得把推测表述为已确认事实；不得为了表现进度跳过必要验证。

### 7.2 极简主义

**Minimum code that solves the problem. Nothing speculative.**

- 不实现需求范围以外的「未来能力」（多声部、云同步、插件体系等在需求出现前不做）。
- 优先复用项目已有模式；不添加无关配置、脚本或依赖。

### 7.3 外科手术式修改

**Touch only what you must. Clean up only your own mess.**

- 只改与当前任务直接相关的文件；不顺手重构附近代码。
- 不擅自统一格式、重命名或调整目录结构。
- 清理由本次修改造成的未使用 import、变量、函数与临时代码。

### 7.4 目标驱动执行

将模糊任务转换为可验证目标。例：

- 「支持简谱解析」→ 先用样例谱写出失败测试 → 实现最小解析器 → 测试通过 → 用真实曲谱回归。
- 「游戏内能演奏」→ 先 dry-run 导出事件序列核对音高/时序 → 再在游戏内按验收清单逐项确认。

### 7.5 测试原则

- 优先跑最相关、最快的定向测试，再按影响范围扩大验证。
- 功能开发完成前必须跑全量回归。
- 修复 bug 原则上补一个能复现的回归测试。
- 无法自动化时，给出可重复的手动验证步骤。
- 不得通过删除、跳过或弱化测试制造「测试通过」；不得声称「全部通过」而未实际运行。

### 7.6 连续失败熔断

对同一个错误最多进行 **2 次有实质差异的修复尝试**（必须有新日志、新证据、新假设或明显不同的方案）。两次仍失败后：停止试错 → 保留现场 → 记录 `ERRORS.md` 与 `errors/ERR-XXX.md` → 向用户汇报现象、已验证假设、两次尝试、当前阻塞与所需信息。

---

## 8. 安全与版本控制规则

### 8.1 安全红线

以下行为绝对禁止，除非规则明确说明可由用户授权；涉及凭据与系统安全的行为，即使用户要求也应先提示风险：

- ❌ 删除或修改 `.git/`
- ❌ 读取、输出、提交或传播密码、Token、API Key、私钥等敏感信息
- ❌ 读写游戏进程内存、修改或替换游戏文件、hook 游戏进程
- ❌ 绕过、探测、干扰反作弊（ACE）机制，或任何形式的反检测规避
- ❌ 执行 `rm -rf /`、磁盘格式化、无边界递归删除等破坏性命令
- ❌ 擅自新增、升级或删除依赖
- ❌ 擅自格式化整个项目
- ❌ 覆盖或丢弃用户已有的未提交改动
- ❌ 未经授权执行 `git reset --hard`、`git clean -fd`、强制 checkout、`git stash`
- ❌ 执行交互式或持续挂起主终端的命令（`vim`、`tail -f`、前台运行 UI/宏等）
- ❌ 伪造命令输出、测试结果、文件内容或执行状态

#### 本项目专属风险提示

- ⚠️ 游戏内使用自动输入存在被反作弊判定的风险，属于用户自担风险的本地使用场景。任何**扩大自动化范围**（例如从乐器演奏扩展到对战操作）的方案变更，必须先向用户提示风险并取得明确同意。
- ⚠️ 长时间自动按键可能被游戏判定为异常行为；默认实现应避免不必要的极高频输入。

### 8.2 建议性约束

除非任务明确要求：

- ⚠️ 不新增 build 脚本或 CI 配置
- ⚠️ 不修改 `.gitignore` 现有规则
- ⚠️ 不修改已标记为 `Accepted` 的 ADR 正文
- ⚠️ 不移动或批量重命名文件
- ⚠️ 不更改公共配置默认值（`config/instrument.json` 的语义变更需 ADR）
- ⚠️ 不引入新的全局工具
- ⚠️ 不提交自动生成的大型产物（`runtime/` 已在 `.gitignore` 中）

### 8.3 版本控制原则

- 开始工作前检查 `git status --short`；本沙盒 `.git` 只读，无法执行时如实说明。
- Agent 不执行 `git init/add/commit/push`，只提供建议命令，由用户在本地终端执行。
- **禁止 `git init`**：沙盒内创建的 `.git` 会导致环境刷新失败（ERR-001）。
- **提交邮箱固定为 noreply**：`332089221+SSSS-TY@users.noreply.github.com`
  （GitHub 已开启邮箱隐私保护，用真实邮箱推送会被 GH007 拒绝，见 ERR-002）。
- 不把用户已有改动归功于本次任务。
- 不使用 `--no-verify`，不重写历史。

---

## 9. 统一完成检查清单

### 9.1 代码与范围

- [ ] 仅修改了与当前任务相关的文件
- [ ] 没有覆盖或丢弃用户已有改动
- [ ] 没有夹带无关重构或格式化
- [ ] 已清理由本次任务引入的未使用代码与临时调试输出
- [ ] 新增的公共函数/类/接口含用途说明
- [ ] 修改文件数量原则上不超过 5 个；若超过，已说明必要性

### 9.2 验证

- [ ] 已确认 Check 命令存在且可执行
- [ ] 已运行与本次改动直接相关的定向测试
- [ ] 已运行 `python -m compileall -q kouqin`（存在 `kouqin/` 时）
- [ ] 功能开发或高影响改动已运行全量回归（`python -m pytest -q`）
- [ ] 涉及游戏内行为的改动已按设计文档的验收清单手动确认，并记录结论
- [ ] 所有声称通过的命令都已实际执行
- [ ] 已记录未执行的检查及原因
- [ ] 已审查 diff（`git diff` / 无法执行时说明）

### 9.3 文档与记录

- [ ] `PROGRESS.md` 状态与代码库一致，`Next Steps` 已更新
- [ ] 阻塞项准确记录
- [ ] 重大架构或业务决策已按 ADR 格式追加到 `DECISIONS.md`
- [ ] 未解决错误已写入 `ERRORS.md` 与 `errors/ERR-XXX.md`
- [ ] 用户可见行为变化已在 `README.md` 或相关设计文档中说明

### 9.4 最终汇报

最终回复至少包含：**完成内容 / 修改文件 / 验证命令及结果 / 未完成项或已知风险 / 建议的下一步**。没有风险或未完成项时明确写「无」，不要省略。

---

## 10. 调试代码规范

Python：

```python
print("// AGENT-DEBUG:", variable)
```

AutoHotkey v2：

```ahk
OutputDebug("// AGENT-DEBUG: " var)
```

清理规则：任务完成前删除本次添加的 `// AGENT-DEBUG` 调试代码；不得删除用户原有且不带此前缀的日志；调试日志中不得出现凭据或个人信息。

---

## 11. 架构决策记录规则

### 11.1 必须追加 `DECISIONS.md` 的情况

- ✅ 引入新的外部依赖
- ✅ 修改运行时契约（`runtime/*.json`、`config/instrument.json` 的字段与语义）
- ✅ 修改核心配置默认值（时值、间隔、热键等）
- ✅ 改变模块职责、进程边界或核心数据流
- ✅ 变更输入注入方式或注入范围
- ✅ 绕过已知缺陷或采用非预期 workaround
- ✅ 引入新的安全、权限或数据隔离策略

### 11.2 通常无需追加的情况

- ❌ 不改变行为的内部重构
- ❌ 修复明显逻辑 bug，且不改变设计
- ❌ 代码格式调整、文档拼写修正
- ❌ 增补已有行为的测试
- ❌ 依照已有决策完成实现

### 11.3 ADR 修改规则

新决策用新编号；不直接改写已 `Accepted` 的历史事实。若要替代旧决策：新建 ADR → 引用旧 ADR → 旧 ADR 状态改为 `Deprecated`/`Superseded` → 说明替代原因与迁移影响。

---

## 12. 错误记录与恢复

### 12.1 何时记录错误

- 连续两次有效修复尝试后仍未解决
- 依赖外部系统、权限或环境而无法继续
- 发现可能影响其他模块但本次无法安全修复的问题
- 构建/测试存在无法解释的非确定性失败
- 为继续开发采用了临时 workaround

### 12.2 错误编号

使用 `ERR-XXX`，按 `ERRORS.md` 现有最大编号递增，不复用编号；索引与详情文件使用相同 ID。

### 12.3 检查失败时的处理

1. 确认失败是否由本次改动导致。
2. 只修复与本次改动直接相关的问题。
3. 修复后重跑失败检查及必要的关联检查。
4. 达到熔断阈值后停止。
5. 不得用 `git stash`、`git reset --hard` 或删文件掩盖失败。
6. 记录错误并请求用户指导。

### 12.4 已解决错误的归档

错误解决且连续 7 天未重现后，可将详情文件移动到 `docs/harness/archive/errors/`，并保证索引链接仍有效。

---

## 13. 当前任务全部完成时

1. **清理**：删除本次添加的调试输出与临时文件/注释，清理无用 import。
2. **文档**：按需更新 `README.md`、`docs/design/` 与配置说明，保证与代码一致。
3. **验证**：运行 `python -m pytest -q` 与 `python -m compileall -q kouqin`，审查最终 diff。
4. **沉淀**：在 `PROGRESS.md` 的 `Completed`/`Archive` 记录任务摘要、关键难点、验证结果与剩余风险。
5. **决策**：有重大架构/业务/技术决策时追加 ADR。
6. **归档**：`PROGRESS.md`/`DECISIONS.md`/`ERRORS.md` 超过 500 行时，将已完成或失效条目移入 `docs/harness/archive/`；不归档 Active/Proposed/Unresolved 条目。
7. **汇报**：按 §9.4 汇报并提示用户创建提交；不自动 commit / push。
