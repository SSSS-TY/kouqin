# 游戏内手动验收清单（v1）

> 用途：覆盖自动化测试无法验证的实机行为（TEST_PLAN §7 的 MAN-01…MAN-07）。
> 环境：游戏机 B，训练场/靶场等安全场景。
> 记录：每条填「观察结果 + 通过/不通过」，不通过时附截图或录像文件名。

| 编号 | 步骤 | 判据 | 结果 | 备注 |
|------|------|------|------|------|
| MAN-01 | 训练场执行 `python -m kouqin play scores\twinkle.kq` | 旋律可辨认、节奏均匀，与参考录音一致 | ✅ 通过（2026-09-25，B 机） | 用户反馈：旋律是小星星、无明显错音、节奏正常、长音正常。注：该曲全为自然音，**未覆盖鼠标修饰键路径** |
| MAN-02 | 播放 `scores\chromatic.kq`（37 音半音阶，覆盖全部 6 种修饰组合） | 音高逐级升高、无跳音或重复、无错音 | ✅ 通过（2026-09-25，B 机） | 用户反馈：运行正常、听感无问题。至此**鼠标修饰键注入路径已验证** |
| MAN-03 | 播放 `scores\long_note.kq`（12 秒长音） | 用户裁决：**放弃断点重按**——重触发接缝必然可闻，比自然衰减更突兀。故 `retrigger_long_notes` **默认关闭**，接受约 10 秒后自然衰减 | ✅ 已裁决（2026-09-25） | 用户原话「long_note.kq 还是有明显中断感，可以考虑放弃这种断点重按的尝试」。能力保留为可选开关，`sustain_limit_ms = 8000` 仍记录实测值 |
| MAN-04 | 播放中按停止按钮；再播放中按 `Ctrl+Alt+K` 急停 | 立即停声；松开后不残留按下的键 | ✅ 通过（2026-09-25） | 用户确认「MAN-04 我试过，没问题」 |
| MAN-05 | 在「校准」页取消勾选「我已核对」并保存 | 「开始」按钮禁用，消息面板出现 `[CP004]`；重新勾选后恢复 | ✅ 通过（2026-09-25） | 用户确认「校准约束没问题」 |
| MAN-06 | 在编辑器里故意把一行写成 `1 9 3` | 消息面板给出行号与错误码 `KQ004`，且「开始」变灰 | ✅ 通过（2026-09-25） | 用户确认「MAN-06 错误提示也没问题」 |
| MAN-07 | 用 `Ctrl+Alt+P` 开始/停止、`Ctrl+Alt+U` 暂停/继续 | 热键生效，且**没有**触发游戏内任何动作（按键被系统吞掉） | ✅ 可用（2026-09-25） | 用户确认「MAN-07 可用」。**附带发现**：暂停停在音符中间会变成长按 → 已修（暂停=静音，见 MAN-10） |
| MAN-08 | 演奏中按 Alt+Tab / Win 键离开游戏 | 立即停声、释放所有按键 | ✅ 通过（2026-09-25） | 用户确认「MAN-08 也没问题」 |
| MAN-09 | 点「开始」后**不切回游戏**（3 秒倒计时结束时前台仍是本界面） | 弹提示且**没有任何按键**打到界面上 | ✅ 通过（2026-09-25） | 用户确认「不切回游戏不会开始」 |
| MAN-10 | 播放中在**某个音按住期间**按 `Ctrl+Alt+U` 暂停，等 2 秒再继续 | 暂停瞬间该音**停止发声**；继续时按剩余时值重新起音 | ✅ 通过（2026-09-25） | 用户确认「没问题了」 |

## 记录人 / 日期 / 客户端版本

- 记录人：用户（B 机实机）+ Agent（自动化）
- 日期：2026-09-25
- 游戏客户端版本：
- 结论：**v1 验收通过**（MAN-01…MAN-10 全部通过；A1–A14 逐条有据可查）
