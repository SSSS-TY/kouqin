# 游戏内手动验收清单（v1）

> 用途：覆盖自动化测试无法验证的实机行为（TEST_PLAN §7 的 MAN-01…MAN-07）。
> 环境：游戏机 B，训练场/靶场等安全场景。
> 记录：每条填「观察结果 + 通过/不通过」，不通过时附截图或录像文件名。

| 编号 | 步骤 | 判据 | 结果 | 备注 |
|------|------|------|------|------|
| MAN-01 | 导入 `tests/fixtures/scores/simple.kq`，在训练场点「开始」 | 旋律可辨认、节奏均匀，与参考录音一致 | 待测 | |
| MAN-02 | 播放 `tests/fixtures/scores/advanced.kq` | 变化音 `#4`/`b3`、附点、休止、连音都能听出来，无错音 | 待测 | |
| MAN-03 | 播放含 ≥2 拍长音的曲目（先保持 `sustain_limit_ms = null`，再填入 P0-7 测得的值重测一次） | 长音不消失；开启重触发后没有明显「断气」感 | 待测 | 依赖 P0-7 |
| MAN-04 | 播放中按停止按钮；再播放中按 `Ctrl+Alt+L` 急停 | 立即停声；松开后不残留按下的键（可在记事本里验证鼠标键状态） | 待测 | |
| MAN-05 | 把 `config/instrument.json` 的 `verified` 改为 `false` 后重启程序 | 「开始」按钮禁用，提示「请先在『校准』页确认三个鼠标键的音程」 | 待测 | 测完改回 `true` |
| MAN-06 | 在编辑器里故意把一行写成 `1 9 3` | 该行标红，消息面板给出行号与错误码 `KQ004` | 待测 | |
| MAN-07 | 用 `Ctrl+Alt+P` 开始/停止、`Ctrl+Alt+U` 暂停/继续 | 热键生效，且**没有**触发游戏内任何动作（按键被系统吞掉） | 待测 | |

## 记录人 / 日期 / 客户端版本

- 记录人：
- 日期：
- 游戏客户端版本：
- 结论：待测

